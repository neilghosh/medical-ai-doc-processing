"""Lab2PHR demo scripts. Each module exposes one public callable used by the
matching agent and the FastAPI layer:

    scripts.ingest_reports.ingest(path)
    scripts.query_index.search(query, k)
    scripts.phr_extractor.extract(image) / .explain(record)
"""
